alias start-db-tunnel='db-tunnel-start'
alias stop-db-tunnel='db-tunnel-stop'
alias list-db-tunnel='print -rl -- ${(k)DB_TUNNELS}'

# Short forms, carried over from the old ~/.dotfiles checkout.
alias start-db='db-tunnel-start'
alias stop-db='db-tunnel-stop'
alias list-db='print -rl -- ${(k)DB_TUNNELS}'
